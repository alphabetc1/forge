interface:
  display_name: "Forge"
  short_description: "Git-backed prompt, research, plan, build, review workflow"
  default_prompt: "Use $forge to turn a coding request into prompt.md, research.md, plan.md, implementation changes, review.md, and stage commits."
